/*
* Test profile — small bundled dataset, for `qc_pipeline` (main.nf).
*/

params {
    raw_adata    = "$projectDir/test/adata_test.h5ad"
    celltype_csv = "$projectDir/test/celltype_test.csv"
    batch        = "plate"
    metadata     = "sample"
}
