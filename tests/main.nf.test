nextflow_pipeline {

    name "Test Workflow main.nf"
    script "../main.nf"

    test("Should wire up and complete a stub run against the bundled test data") {

        options "-stub-run"

        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success
        }

    }

}
